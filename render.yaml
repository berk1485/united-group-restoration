services:
  - type: web
    name: united-group-restoration
    env: node
    plan: free
    buildCommand: npm install --production
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 20
